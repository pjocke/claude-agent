apiVersion: v1
kind: Service
metadata:
  name: incident-agent
  labels:
    app: incident-agent
spec:
  selector:
    app: incident-agent
  ports:
    - port: 80
      targetPort: 8000
      protocol: TCP
